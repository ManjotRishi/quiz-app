{
  "name": "daily-quizz-backend",
  "private": true,
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
  "deploy": "firebase deploy --only functions",
    "emulate": "npx firebase emulators:start --only functions"
  },
  "dependencies": {
    "axios": "^1.6.1",
    "firebase-admin": "^11.11.1",
    "firebase-functions": "^4.9.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
